language: groovy

sudo: false

jdk: oraclejdk8

cache:
  directories:
    - ~/.grails

before_script:
  - npm install -g bower eslint

script:
  - eslint web-app/js/source --inline-config=false
  - ./grailsw codenarc
  - cat static-analysis.txt
  - ./grailsw war
